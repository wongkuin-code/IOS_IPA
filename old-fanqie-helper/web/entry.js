import { AppRegistry } from 'react-native';
import App from '../App';

AppRegistry.registerComponent('FanqieHelper', () => App);
AppRegistry.runApplication('FanqieHelper', {
  rootTag: document.getElementById('root'),
});
