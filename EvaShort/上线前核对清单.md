# EvaShort 上线前核对清单（剩余阻塞 + 账号收尾）

> 生成于 2026-09-01。结论：依赖已重装、iOS 导出已验证通过（构建管线健康）。以下为 ASC 侧需人工完成的步骤，按优先级排列。
> 带 ⚙️ 的步骤可由我代执行（需你提供对应凭证/文件）；带 🔑 的步骤必须由你在 App Store Connect 网页完成。

---

## Step 0 — 主账号切换收尾（🔑 需你确认账号关系）

本地 `keys/apple-dev.txt` 已改 `AppleID = taoxi110@qq.com`。`eas.json` 的 ASC 提交密钥（`6XTPZ5CBDW` / Issuer `761a52ec-...` / `AuthKey_6XTPZ5CBDW.p8` / App `6802204407`）**已确认三枚 .p8 均在位且曾成功上传，无需重发**（2026-09-01 修正：旧版"必须重发"结论已作废）。

**待你确认一件关键事**：`taoxi110@qq.com` 与之前打包上传所用的账号，是否为**同一个 Apple 开发者账号**？
- 若是同一账号（仅换了主邮箱/别名）→ ASC API Key 仍对 App 有权限，**直接 `eas build` + `eas submit` 跑通即证，无需任何改动**。
- 若是独立新账号、App 仍归旧账号 → API Key 仍可对旧账号的 App 提交（提交鉴权看 Key 本身），大概率也能跑通；若要把 App 彻底转到 taoxi110 名下，再在 ASC 里做所有权转移或重建 App。

> App 专用密码：磁盘上只有旧账号的 `xkze-zcvt-msnb-sgrt`（已失效），`taoxi110` 的密码不存在。但 `eas submit` 走 API Key 鉴权、大概率不依赖它；`eas build` 同样优先用 API Key。**此密码为非必需兜底凭证，可后补。** 真要补：去 appleid.apple.com → 登录 taoxi110 → 登录与安全 → App 专用密码 → 生成，把 `xxxx-xxxx-xxxx-xxxx` 发我写进 `keys/apple-dev.txt` 即可。

---

## Step 1 — IAP 商品 `2.99` 送审（🔑 ASC 网页）

代码侧已对齐：`src/iap/iap.js` → `VIP_PRODUCT_ID = '2.99'`，**非消耗型**（一次性解锁，lifetime）。

App Store Connect 操作：
1. 我的 App → EvaShort → **「App 内购买项目」** → 若还没有 `2.99`，点「创建」→ 类型选 **非消耗型项目（Non-Consumable）**，参考名称/产品 ID 填 `2.99`。
2. 填写：显示名称（如 `Premium Dramas`）、描述、定价等级（❗ 见下方提示）、截图（IAP 需一张该功能截图）。
3. 提交审核：**勾选「提交至 App Review」**，状态变「Waiting for Review」。
4. 回到「App 版本」页 → 在 **App 内购买项目** 区域把 `2.99` **勾选**加入本次提审。

⚠️ 提示：App 内 paywall 文案写的是「¥1」(`ProfileScreen.js:133`)，但商品 ID 是 `2.99`。请在 ASC 把 `2.99` 的价格等级设为与你想展示的一致（若实际卖 ¥1，则 ASC 定价也设为对应 tier），避免「展示价格 ≠ 实际扣费」的 2.1(b) 风险。

---

## Step 2 — 年龄分级（2.3.6）（🔑 ASC 网页）

App Store Connect → 我的 App → EvaShort → **「年龄分级」** → 「编辑」→ 如实填写问卷：
- 当前 App 是短剧播放（含可能浪漫/戏剧化情节），**不要**填 4+ 伪装全年龄。
- 建议基线：若内容无露骨性描写 → 选 **12+**，并在对应项勾「偶尔/轻微（Infrequent/Mild）」；若含任何暗示性内容 → 直接 **17+**。
- 分级必须与 App 实际内容一致，否则 2.3.6 拒审。

（具体勾选项需你按剧集实际内容判断；如不确定，倾向填高一级更稳妥。）

---

## Step 3 — 真实截图（2.3.3）（⚙️ 我出尺寸清单，🔑 你真机截）

**当前 `mytool/assets/screenshots/` 全是 8/18 的旧阅读 App 图（shelf/reader），且含 iPad 图——EvaShort 是 iPhone-only（`supportsTablet:false`），iPad 图本就不需要，且旧图必须全部替换。**

### 尺寸规范（2026 最新，iPhone-only）
| 显示屏 | 竖屏像素 | 要求 |
|---|---|---|
| **6.9″（16 Pro Max）** | **1320 × 2868** | ✅ 主推（必传至少一组） |
| 6.7″（15/16 Plus） | 1290 × 2796 | 建议补传 |
| 6.5″（13/14 Pro Max 等） | 1284 × 2778 | 可选 |

- 格式：sRGB **PNG 或 JPEG，禁止透明度/Alpha**。
- 状态栏必须干净（无真实运营商名、无低电量红）。
- 语言与 App 一致 → **全英文界面截图**。
- 至少 1 张，最多 10 张；建议 5–6 张讲清故事。

### 必须捕获的真实画面（用 TestFlight 构建或 Expo dev 真机）
1. 首页信息流（剧集封面网格）
2. 剧集详情页
3. **播放器正在播真实视频的帧**（`expo-video` 真播，非占位）
4. Premium 付费墙（paywall）
5. Profile / 设置页（含 Privacy Policy、Support、Delete Account 入口）

导出时按上表尺寸导出，覆盖 `mytool/assets/screenshots/` 下对应文件（旧 iPad 图可删）。

---

## Step 4 — 授权书上传（5.2.3）（🔑 ASC 网页 + 你核验条款）

文件已就位：`legal/授权书_火华短剧.pdf`（0.5MB 扫描件，需你本人对照原件核验）。

**上传前请确认授权书涵盖以下 7 项条款**（缺则补签）：
1. 展示授权（可在 App 展示该剧）
2. 转码/存储授权（服务器转码、存储）
3. 收费授权（含 IAP 内购收费）
4. 全球分发授权（App Store 全球上架）
5. 肖像权授权（演员肖像）
6. BGM/音乐授权
7. 授权期限与可撤销条款

App Store Connect → 我的 App → EvaShort → **「App 审核信息」** → 「备注」粘贴授权要点，并把 PDF 作为**附件**上传（或放在可公开访问的 URL 供审核员下载）。

---

## Step 5 — 隐私营养标签（App 隐私）（🔑 ASC 网页，须与隐私政策一致）

App Store Connect → 我的 App → EvaShort → **「App 隐私」** → 数据收集与使用，按隐私政策正文填：
- **Identifiers**：用户名 / 用户 ID
- **Purchases**：IAP 交易
- **Usage Data**：观看历史、搜索历史
- **Diagnostics**：**不勾**（政策称日志不用于追踪）
- 用途一律 **App Functionality**；**Tracking = 否**

> 隐私政策网址已在「App 隐私 → 编辑隐私政策」填 `https://api.haoweimedia.cn/evashort/privacy-policy.html`（实测 200）。

---

## Step 6 — 重新提审（🔑 ASC 网页）

完成 Step 1–5 后：
1. 我的 App → EvaShort → **「App Store」** → 版本编辑器 → 确认截图、描述、隐私、IAP、年龄分级均已就位。
2. 若尚未上传构建：先 `eas build --platform ios --profile production`（需 Step 0 新密钥/账号就绪），再 `eas submit`。
3. 点「提交审核」。

---

## 已完成的元数据（无需重传构建）
| 字段 | 值 | 状态 |
|---|---|---|
| 隐私政策网址 | `https://api.haoweimedia.cn/evashort/privacy-policy.html` | ✅ 200 |
| 技术支持网址 | `https://api.haoweimedia.cn/evashort/support.html` | ✅ 200 |
| 版权 | `2026 Haowei (shenzhen) Cultural Media Co., Ltd` | ✅ |
| 联系邮箱（两页） | `taoxi110@qq.com` | ✅ |
| node_modules 依赖 | 已重装 + 字体补齐，iOS 导出 EXIT=0 | ✅ |

## 阻塞总览
- ⚙️/🔑 Step 0 新 ASC 密钥（你提供后我改 eas.json）
- 🔑 Step 1 IAP `2.99` 送审
- 🔑 Step 2 年龄分级
- ⚙️/🔑 Step 3 真实截图（我出清单，你截）
- 🔑 Step 4 授权书上传 + 条款核验
- 🔑 Step 5 隐私营养标签
- 🔑 Step 6 重新提审
